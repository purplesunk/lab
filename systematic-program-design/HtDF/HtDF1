;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname HtDF1) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
;; HtDF

;; Signature
;; Declare values the function consumes and produces

;; Purpose
;; Explain the function

;; Stub:
;; Define function and return a corret value 

;; Examples
;; Help ilustrate behaviour

;; --------------------

;; Number -> Number
;; produce 2 times the given value
(check-expect (double 3) 6)
(check-expect (double 2.4) (* 2 2.4))

;(define (double n) 0) ; this is stub

;(define (double n) ; this is template
;  (... n))

(define (double n)
  (* 2 n))
